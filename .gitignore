node_modules
dist
dist-verify
.verify-received.ndjson
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
*.tsbuildinfo

# Scratch git worktrees the agent harness creates for isolated runs. They are
# temporary checkouts of this same repo and are removed when the run ends.
# Project settings under .claude/ are still tracked; only these are not.
.claude/worktrees/

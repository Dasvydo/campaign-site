# FUNNEL-HANDOFF — pointer

**The contract lives in `ad-engine/docs/FUNNEL-HANDOFF.md`.** One copy, so the two
cannot drift.

This file was briefly a full duplicate of it (2026-09-16, branch
`claude/confident-ritchie-ggzjjm`). That was a mistake — two sessions wrote the
same document at the same path on two branches, which is the exact failure a
single canonical copy exists to prevent. Reduced to a pointer, matching the
design already chosen on `claude/great-bell-10ca2r`.

> ⚠️ Branch `claude/great-bell-10ca2r` carries a longer version of this pointer
> with detail this one does not: `src/lib/attribution.ts`, `src/lib/contract.ts`
> and the `numbers` block. **Prefer that one where they differ**, and fetch it:
>
> ```bash
> git fetch origin claude/great-bell-10ca2r
> git show origin/claude/great-bell-10ca2r:docs/FUNNEL-HANDOFF.md
> ```
